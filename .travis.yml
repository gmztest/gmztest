language: cpp
compiler: gcc
os: linux
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -y cmake g++
  - sudo apt-get install -y libboost-all-dev libopenblas-dev opencl-headers ocl-icd-libopencl1 ocl-icd-opencl-dev zlib1g-dev
script: cd ./src && make
